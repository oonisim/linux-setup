#jinja2: lstrip_blocks: "True", trim_blocks: "True"
127.0.0.1	localhost
#127.0.1.1	aim-Precision-7510

{% for group in cluster|list %}
    {% for host in group.hosts|list %}
{{ host.ip }} {{ host.hostname }}
    {% endfor %}
{% endfor %}

::1     ip6-localhost ip6-loopback
